

activity_niger_delta_new_yam_festival = {
	expiration_days = 365
	solo = yes
	color = { 0.01 0.60 0.27 1 } # African green
	on_spawn = {
		invite_character_to_activity = activity_owner
		accept_invitation_for_character = activity_owner

	}
	on_activate = {
		activity_owner = {
			trigger_event = { # Sacrifice a prisoner?
				id = niger_delta.0034
				days = { 10 15 }
			}
			trigger_event = { # Festival starts
				id = niger_delta.0032
				days = { 25 40 }
			}
		}
	}
	on_complete = {
		activity_owner = {
			remove_character_flag = planning_an_activity
		}	
	}
	on_expire = { # Should never happen - kept as fallback for weirdness only
		activity_owner = {
			remove_character_flag = planning_an_activity
		}
	}
}

activity_niger_delta_olojo_festival = {
	expiration_days = 365
	solo = yes
	color = { 0.94 0.20 0.25 1 } # African red
	on_spawn = {
		invite_character_to_activity = activity_owner
		accept_invitation_for_character = activity_owner

	}
	on_activate = {
		activity_owner = {
			if = {
				limit = {
					has_title = title:d_ooni_ife
				}
				trigger_event = {
					id = niger_delta.8888
					trigger_on_next_date = 10.1 # Ooni goes into seclusion and communes with the ancestors
				}
			}
			trigger_event = { # Approximate
				id = niger_delta.8888
				trigger_on_next_date = 10.10 # Traditionally began around mid October
			}
		}
	}
	on_complete = {
		activity_owner = {
			remove_character_flag = planning_an_activity
		}	
	}
	on_expire = { # Should never happen - kept as fallback for weirdness only
		activity_owner = {
			remove_character_flag = planning_an_activity
		}
	}
}

activity_niger_delta_beaded_crown = {
	expiration_days = 180
	solo = yes
	color = { 0.78 0.89 0.88 1 } # Glass blue (kind greenish tho?)
	on_spawn = {
		invite_character_to_activity = activity_owner
		accept_invitation_for_character = activity_owner

	}
	on_activate = {
		activity_owner = {
			trigger_event = {
				id = niger_delta.8888
				days = { 15 30 }
			}
		}
	}
	on_complete = {
		activity_owner = {
			remove_character_flag = planning_an_activity
		}	
	}
	on_expire = { # Should never happen - kept as fallback for weirdness only
		activity_owner = {
			remove_character_flag = planning_an_activity
		}
	}
}