

RICE_khwarezm_kala_fortress_cost_value = {
	value = 25
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level <= 1 }
		}
		add = 5
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 2 }
		}
		add = 10
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 3 }
		}
		add = 15
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 4 }
		}
		add = 20
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 5 }
		}
		add = 25
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 6 }
		}
		add = 30
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 7 }
		}
		add = 35
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 8 }
		}
		add = 40
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 8 }
		}
		add = 45
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 8 }
		}
		add = 50
	}
	min = 25
}

RICE_khwarezm_kala_fortress_prestige_value = {
	value = 50
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level <= 1 }
		}
		add = 5
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 2 }
		}
		add = 10
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 3 }
		}
		add = 15
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 4 }
		}
		add = 20
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 5 }
		}
		add = 25
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 6 }
		}
		add = 30
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 7 }
		}
		add = 35
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 8 }
		}
		add = 40
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 8 }
		}
		add = 45
	}
	every_held_title = {
		limit = {
			tier = tier_county
			title_province = { fort_level = 8 }
		}
		add = 50
	}
	min = 25
	max = 200
}

RICE_khwarezm_destroy_cult_center_value = {
	value = 50
	if = {
		limit = {
			NOT = { religion = religion:zoroastrianism_religion }
		}
		every_held_title = {
			limit = {
				tier = tier_county
				# has_county_modifier = RICE_zoroastrian_cult_center - list them all out
			}
			add = 50
		}
	}
	else_if = {
		limit = {
			faith = { has_doctrine_parameter = rice_unlock_destroy_cult_centers }
			religion = religion:zoroastrianism_religion
		}
		every_held_title = {
			limit = {
				tier = tier_county
				# has_county_modifier = RICE_zoroastrian_cult_center - list them all out
			}
			add = 50
		}
	}
	min = 50
}
