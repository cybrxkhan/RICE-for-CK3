# Dummy copies of real effects in SoW. Should be overwritten based on file name regardless of load order. 
# However, RICE should still be loaded before SoW because CCU relies on that and is bundled inside SoW

# Put in the on complete block of every building that adds `max_garrison = xxx`
apply_province_garrison = { }

# Put in the on complete block of every building that adds `levy = yyy`
apply_province_levy = { }